# 2026-09-20

## 修复浏览器测试初始化竞态

复查发现昨天相同提交的 PR CI 成功，但 push CI [35416488808](https://github.com/dcr0coof/agent-platform/actions/runs/35416488808) 的天气依据测试失败，错误为 `Cannot read properties of undefined (reading 'id')`。因此昨天“最终 CI 全部通过”的概括范围过宽：已核实的是 PR 流水线，不是所有触发的运行。

本地给会话创建 POST 注入 1 秒延迟，旧测试再次在读取 `sessions[0].id` 处失败。`page.goto` 结束只代表页面导航完成，不保证异步会话创建结束；修复以会话列表出现一项作为就绪条件，未增加重试或用固定等待掩盖问题。延迟仅用于模拟慢初始化，作为回归保留。

生产构建与全部 10 项 Playwright/Chrome 测试通过。没有生产代码或 Go 改动，无需重复本地 Go 检查。本次验收将同时核对最终 push 与 PR CI。

关联 [Issue #18](https://github.com/dcr0coof/agent-platform/issues/18)，分支 `codex/weather-evidence-ready`，依赖 PR #17 的 `codex/weather-evidence-panel`。优先修 CI，本次未新增天气功能。下一步继续天气证据与地点确认。
