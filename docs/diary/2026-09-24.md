# 2026-09-24

## 按出行暂存未发送消息

基线为未合并 PR #25 的 `codex/forecast-local-date`（3f9064c）；开始时工作区干净，远端最近 24 小时无提交，最新 push/PR CI 成功。关联 [Issue #26](https://github.com/dcr0coof/agent-platform/issues/26)，分支 `codex/trip-message-drafts`。

问题：切换/新建出行会清空正在组织的消息。现按会话 ID 在页面内暂存草稿，切换成功才保存/恢复，加载失败保留原输入；同会话的数据更新不触碰输入，服务器接受发送后删除草稿。提示刷新/关闭后清除，不写服务器或浏览器持久存储，也不把未发送文本交给模型。

验证：`npm run build`、`PLAYWRIGHT_CHANNEL=chrome npm run test:e2e` 全部 12 项通过（1 worker）；新增两个浏览器用例覆盖双会话独立草稿、新建、保存、发送清空、失败后恢复及页面刷新。原有超时幂等重试、取消、天气依据隔离和移动端无横向溢出检查通过。本次无 Go 变更，未重复本地 Go 全量检查；CI 将验证完整仓库。

限制：仅当前页面内暂存，不保证刷新恢复；约束仍独立保存。下一步继续攻略资料与上下文闭环。PR/提交发布后补充链接，不自动合并。

发布记录：[代码提交 78f0bcb](https://github.com/dcr0coof/agent-platform/commit/78f0bcb8d7aee24d700cca63b2115347804f1151) 已推送并核对 SHA；[PR #27](https://github.com/dcr0coof/agent-platform/pull/27) 已创建。最终 CI 以 PR 更新为准。
