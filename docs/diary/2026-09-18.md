# 2026-09-18

## 天气观测证据

为 now 查询保留来源、Location ID、获取/API 更新/观测时间和观测年龄。未知、格式非法与未来时间均明确标注，不把重新获取旧观测当成重新观测，也不设置未经论证的通用天气过期阈值。

本地 `go test -p=2 ./...`、`go vet -p=2 ./...`、`go build -p=2 ./...` 通过；固定 HTTP 响应和受控时间覆盖分钟/秒精度、时区、旧观测、缺失/非法/未来时间。未调用真实天气服务，未在本机执行 race；由 Linux CI 覆盖。

关联 [Issue #14](https://github.com/dcr0coof/agent-platform/issues/14)，基于 PR #13 的 `codex/weather-date-coverage`，新分支为 `codex/weather-observation-evidence`。整个天气影响与活动备选 Issue #4 仍未完成。

下一步：在 Web 侧展示可定位的天气证据；Provider v1 迁移需独立验证，不能只替换 URL。
