# 2026-09-23

## 目的地当地日期约束

开始时工作区干净，最近 24 小时远端无新提交；上一版 d33938d 的 push / PR CI 均成功。本次基于未合并 PR #23 的 `codex/precipitation-alternatives`，使用 `codex/forecast-local-date`，关联 [Issue #24](https://github.com/dcr0coof/agent-platform/issues/24)。

问题与改动：刚更新的响应中若残留昨日记录，原规则仍会给出新活动建议。先用 HTTP 回归复现历史日期和未知时区仍给建议的行为，再保留城市查询选定地点的 IANA 时区，按获取时的目的地当地日期判定活动日期。历史日期保留原始预报但停止建议；缺失/非法/Local 时区也停止建议。输出时区与当地日期作为依据，使用标准库 tzdata 支持独立 Windows/Linux 运行。

验证：`go test -p=2 ./...`、`go vet -p=2 ./...`、`go build -p=2 ./...`、`git diff --check` 通过；补充完整工具轮次中的时区断言后，该集成测试再运行通过。固定时钟覆盖上海/洛杉矶跨日、纽约冬夏令时、今日/昨日/未来及未知时区；HTTP 检查原始降水保留，明确 ID 选择保留对应时区。既有需要未来日期的集成样本改为相对运行日，避免日历推进造成无关失败。本机不支持 race，Linux CI 覆盖，最终 CI 待核验。

限制：只修活动建议的日期有效性，不提供历史天气 API，不保证 Provider 时区/数据真实，也不保证模型遵从。时区规则需随工具链维护；未进行真实天气账号联调。下一步继续具体备选与攻略资料闭环，不将这次修复视为整个 Issue #4 完成。

发布记录：[代码提交 6cae089](https://github.com/dcr0coof/agent-platform/commit/6cae089c4eaed525fefa4271e27ad543cee0baf0) 已推送并核对远端 SHA；[PR #25](https://github.com/dcr0coof/agent-platform/pull/25) 已创建。记录时最终 CI 尚待完成，以 PR 更新为准，未自动合并。
