# 2026-09-21

## 城市候选澄清

目标：避免同名或模糊地点被第一条搜索结果替代。开始时工作区干净；最近 24 小时远端无新提交；上一版 7182240 的 push / PR CI 均成功。基于未合并 PR #19 分支 `codex/weather-evidence-ready`，本次分支 `codex/weather-city-ambiguity`，不假定 master 已包含前置功能。

关联 [Issue #20](https://github.com/dcr0coof/agent-platform/issues/20)。城市搜索出现多个不同 ID 时，工具返回候选名称、行政区、国家及 ID，请用户选择，停止天气请求。明确 ID 可选择非第一候选；重复 ID 去重，缺失 ID 拒绝使用。工具描述与调用文档同步。

验证：先添加固定 HTTP 回归，旧实现复现错误地查询第一项；修复后 `go test -p=2 ./...`、`go vet -p=2 ./...`、`go build -p=2 ./...`、`git diff --check` 全部通过。两种查询类型共 14 个选择样例覆盖歧义、明确 ID、单候选、重复/缺失 ID；歧义时断言天气请求为零。本机 CGO_ENABLED=0，竞态检测由 Linux CI 执行。前端未变，远端 CI 仍验证 Web 构建和浏览器测试。

限制：保守策略可能增加澄清轮次；尚无持久化选择或强制用户确认机制，不保证模型不会自行重试；没有真实天气账号联调，也未完成整个 Issue #4。下一步天气备选与攻略证据闭环仍按依赖推进。发布与最终 CI 以关联 PR 更新为准。
