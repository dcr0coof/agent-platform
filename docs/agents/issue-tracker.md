# Issue tracker

Repository: `dcr0coof/agent-platform` on GitHub. Use the official API or an authenticated GitHub CLI; never use the account password or embed credentials in the remote URL.

Before creation, inspect existing open/closed issues and pull requests to avoid duplicates. Publish dependency issues first, then reference their actual numbers. Each implementation PR describes behavior, validation and limitations and links its Issue. Keep shared project plans current as scope changes.

The owner explicitly authorized routine publishing and asked the agent to choose the breakdown. Additional approval of routine Issue wording is unnecessary. Publishing blockers must be reported accurately; local drafts live in `docs/github-drafts/` until successfully published.
